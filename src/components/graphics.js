import React, { Fragment } from 'react';
import './fieldsets.css';

const GraphicsRanges = ({
  id,
  label,
  onChange
}) => (
  <fieldset>
    <legend>Audio</legend>
  </fieldset>
);

export default GraphicsRanges;
